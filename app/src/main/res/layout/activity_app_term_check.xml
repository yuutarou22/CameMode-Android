<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	android:background="@color/colorFragmentBackground"
	tools:context=".AppTermCheckActivity">
	
	<ScrollView
		android:id="@+id/scrollView3"
		android:layout_width="match_parent"
		android:layout_height="0dp"
		android:layout_marginHorizontal="@dimen/userinfo_regist_layout_margin_horizontal"
		android:background="#ffffff"
		app:layout_constraintBottom_toTopOf="@+id/app_term_check"
		app:layout_constraintTop_toTopOf="parent">
		
		<TextView
			android:id="@+id/textView9"
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:text="@string/app_term_contents"
			android:textColor="#111111" />
	</ScrollView>
	
	<CheckBox
		android:id="@+id/app_term_check"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:text="利用規約に同意する"
		app:layout_constraintBottom_toTopOf="@+id/app_term_checked_button" />
	
	<Button
		android:id="@+id/app_term_checked_button"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:layout_marginBottom="8dp"
		android:text="次へ進む"
		android:background="@color/colorNegative"
		app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>